// models/ScribeRequest.js
// import mongoose from "mongoose";

// const scribeRequestSchema = new mongoose.Schema({
//   exam_request: { type: mongoose.Schema.Types.ObjectId, ref: 'ExamRequest', required: true },
//   scribe: { type: mongoose.Schema.Types.ObjectId, ref: 'Scribe', required: true },
//   status: { type: String, enum: ['pending', 'accepted', 'rejected'], default: 'pending' },
//   created_at: { type: Date, default: Date.now }
// });

// export const ScribeRequest = mongoose.model("ScribeRequest", scribeRequestSchema);
